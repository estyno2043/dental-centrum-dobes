/**
 * Kept as the name the rest of the app already imports. Everything it used to
 * declare now lives in `serviceMorph.ts`, which both halves of the animation
 * share — the half that grows the photograph into the page and the half that
 * shrinks it back out.
 */
export {
  BACKDROP_ATTRIBUTE,
  BACKDROP_CREAM,
  BACKDROP_FILTER,
  BACKDROP_SCRIM,
  CARD_PHOTO_ATTRIBUTE,
  MORPH_EASING,
  MORPH_MS,
  SETTLE_MS,
} from "./serviceMorph";
