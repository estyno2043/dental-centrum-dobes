import { NavDemo } from "@/components/navdemo/NavDemo";

/**
 * Temporary route for choosing the navigation reveal. Delete along with
 * `components/navdemo` once a variant is picked.
 */
export default function NavDemoPage() {
  return <NavDemo />;
}
